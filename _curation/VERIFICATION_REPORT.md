# Verification report — 2026-05-18

## Summary
- Folders inspected: 13
- Structure: 13 pass, 0 fail
- Style: 13 pass, 0 fail
- Runtime (39 example scripts): 39 pass, 0 fail

All 13 folders match `FOLDER_SPEC.md` exactly. Every example script (curl, Python, TypeScript) ran end-to-end against the live upstream APIs and exited 0. Stdout was sampled from three folders (`fhir-hapi-public-r4`, `global-openfda`, `uk-ons-beta-api`) and confirmed to produce meaningful output, not empty success.

## Structure issues

None. Every folder contains exactly: `README.md`, `endpoints.md`, `examples/curl.sh` (shebang `#!/usr/bin/env bash` present), `examples/example.py`, `examples/example.ts`, `examples/sample-response.json`. No extra files, no subfolders inside `examples/`.

## Style issues

None substantive. Notes for the polish phase:

| Folder | Note | Severity |
|---|---|---|
| (all `endpoints.md`) | Inline curl examples use unlabeled code fences (` ``` `). This matches the template in `_curation/FOLDER_SPEC.md` (line 88) verbatim, but the style rule "Code fences declare language" would suggest ` ```bash `. Spec ambiguity — flag for the curator, not a fail. | Cosmetic |
| `uk-data-gov-uk-ckan/README.md` line 21 | The word "usually" appears in factual prose describing CKAN dataset file formats. Not hedging about the API itself; describing real heterogeneity of upstream resources. Acceptable. | None |
| `global-ebi-ols4/examples/sample-response.json` | Contains the medical term "beta-agonist" inside captured response data. Not hedging; it is real upstream content. Acceptable. | None |
| `uk-ons-beta-api/*` | Uses "Beta" extensively because that is the actual product name (ONS Beta API). Spec exception applies. | None |

Forbidden-word scan (`Claude`, `Anthropic`, `LLM`, `AI assistant`, `agent`, `generated by`, `with the help of`): zero hits across `apis/`, `README.md`, `CONTRIBUTING.md`.

Non-ASCII scan: zero hits across `apis/`. No emojis anywhere.

## Runtime failures

None. Full result matrix:

| Folder | curl | py | ts |
|---|---|---|---|
| fhir-hapi-public-r4 | 0 | 0 | 0 |
| global-clinicaltrials-gov-v2 | 0 | 0 | 0 |
| global-disease-ontology | 0 | 0 | 0 |
| global-ebi-ols4 | 0 | 0 | 0 |
| global-europepmc | 0 | 0 | 0 |
| global-ncbi-eutils | 0 | 0 | 0 |
| global-nlm-clinical-tables | 0 | 0 | 0 |
| global-openfda | 0 | 0 | 0 |
| global-rxnav-rxnorm | 0 | 0 | 0 |
| global-who-gho-odata | 0 | 0 | 0 |
| uk-data-gov-uk-ckan | 0 | 0 | 0 |
| uk-ons-beta-api | 0 | 0 | 0 |
| us-cdc-socrata | 0 | 0 | 0 |

Stdout snippets confirming real output (not empty exits):

- `fhir-hapi-public-r4/example.py` prints a parsed FHIR Bundle (`type: searchset`, 3 Patient resources with id/name/gender/birthDate).
- `global-openfda/example.ts` prints `Total ibuprofen adverse-event reports: 215,343` followed by the top 10 reactions with counts.
- `uk-ons-beta-api/curl.sh` returns three real ONS responses including the weekly-deaths-region dataset metadata and one resolved observation (E92000001, 2025 week 52, all causes → 6906).

Node version: v25.5.0. Python: 3.14.3. `npx tsx` resolved cleanly each call.

## Fixes applied during verification

None. No example scripts needed code changes — every folder-builder's examples ran clean on first attempt.

## Catalogue updates applied

All three updates landed in `_curation/catalogue.json`:

1. **`global-rxnav-rxnorm`**: removed the `/interaction/interaction.json` entry from `key_endpoints`, added `/approximateTerm.json` (example_params: `term=lipitor, maxEntries=5`) and `/ndcstatus.json` (example_params: `ndc=00071015523`). Updated the `gotchas` text — the previous wording said the DDI endpoint was "deprecated for new data in 2022 but the endpoint still responds with the legacy dataset"; new wording states it has been removed entirely and now returns 404.
2. **`global-disease-ontology`**: `base_url` changed from `https://disease-ontology.org/api` to `https://api.disease-ontology.org/v1`; `docs_url` changed from `https://disease-ontology.org/resources/api-documentation` to `https://disease-ontology.org/do-kb/api_doc`; `key_endpoints` paths updated to `/v1/terms/{DOID}` and `/v1/terms/search`. The folder's example scripts already pointed at the new host, so the catalogue is now consistent with what shipped.
3. **`uk-ons-beta-api`**: the observations endpoint `example_params` changed from `{time: "*", geography: "K04000001"}` (returned 0 observations) to `{time: "2025", geography: "E92000001", week: "week-52", causeofdeath: "all-causes"}` — confirmed live to return one real observation (value `6906`).

## Recommendations for polish phase

1. **Top-level README**: not in scope for this verifier, but if you want a uniform table of the 13 APIs grouped by region (UK/Global/US/EU) and by category, the catalogue carries enough metadata to generate one. Current top-level README is 1.4 KB — consider linking the 13 folders directly.

2. **endpoints.md code-fence language**: the spec template uses unlabeled fences for inline curl one-liners. If you want strict ` ```bash ` everywhere, that is a one-pass `sed` across all 13 `endpoints.md` files plus a spec update. Either choice is defensible; pick one for consistency.

3. **No APIs need to be promoted or demoted**. All 13 are reachable, stable, and have meaningful sample responses. The two catalogue-known-fragile endpoints (RxNav DDI, Disease Ontology old base) are now corrected. No "this API is broken since curation" findings.

4. **FHIR sandbox data quirks**: `fhir-hapi-public-r4`'s sample patient name contains a Latin-1-encoded "ñ" that renders as `NuÃ±ez` — this is real upstream data from the public test server, not a bug in the folder. The README already covers "data is public-test, schema may include odd user-submitted resources". No action needed.

5. **NCBI etiquette parameters**: `global-ncbi-eutils` examples correctly send `tool=` and `email=` query params. Worth keeping as the canonical pattern if any future folder calls into NCBI.
